{
  "name": "public",
  "version": "1.0.0",
  "description": "let node.js run on ports under 1024: setcap 'cap_net_bind_service=+ep' /usr/bin/node",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "console-stamp": "^0.2.7",
    "twitter": "^1.7.1",
    "websocket": "^1.0.28"
  }
}
