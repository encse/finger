{
  "name": "public",
  "version": "1.0.0",
  "description": "A simple finger service that returns latest tweets for a user. Works on a TCP port (e.g. the classic port 79) and also on a websocket.",
  "main": "app.js",
  "scripts": {
    "service": "npx nodemon --watch 'src/**/*.*' --watch 'public/**/*.*' --exec 'npx ts-node src/app.ts'"
  },
  "author": "encse",
  "license": "ISC",
  "dependencies": {
    "@types/twitter": "^1.7.1",
    "@types/websocket": "^1.0.5",
    "express": "^4.18.2",
    "node-fetch": "^2.6.7",
    "ts-node": "^10.7.0",
    "twitter": "^1.7.1",
    "websocket": "^1.0.28"
  },
  "devDependencies": {
    "@types/express": "^4.17.14",
    "@types/node-fetch": "^2.6.2"
  }
}
